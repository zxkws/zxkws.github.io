{
  "name": "config-center",
  "version": "1.0.0",
  "description": "Micro-app Configuration Center",
  "scripts": {
    "dev": "npx http-server dist -p 5175 --cors -c-1",
    "build": "mkdir -p dist && echo '{\"microApps\":[],\"version\":\"1.0.0\"}' > dist/micro-apps.json && printf '<!doctype html><title>config-center placeholder</title>' > dist/index.html"
  },
  "license": "MIT"
}
