{
  "name": "@main/app",
  "version": "3.3.2",
  "description": "主应用",
  "dependencies": {
  },
  "devDependencies": {
  },
  "scripts": {
    "dev": "NODE_ENV=development webpack serve -c ./config/webpack.config.js --mode development",
    "build": "NODE_ENV=development webpack -c ./config/webpack.config.js --mode=production",
    "postbuild": "cp -rf build/* ../ && cp build/index.html ../404.html",
    "lint": "npm run eslint && npm run stylelint",
    "eslint": "eslint --cache --ext .js,.jsx,.ts,.tsx ./",
    "eslint:fix": "npm run eslint -- --fix",
    "stylelint": "stylelint \"**/*.{css,scss,less}\""
  }
}