import { cloneDeep, quickSort } from './index.js'

// const obj = {date: new Date(), regex: new RegExp('a','ig')}

// const objClone = cloneDeep(obj);

// console.log(objClone, '-----------' ,obj)

quickSort([3,2,1])