{
  "name": "textdifference",
  "version": "0.0.1",
  "description": "Text Difference Tool",
  "scripts": {
    "dev": "npx http-server -p 5174 --cors -c-1",
    "build": "mkdir -p dist && cp -r *.html *.js dist/"
  },
  "license": "MIT"
}
